<Window x:Class="excelConvert.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:excelConvert"
        xmlns:viewModels="clr-namespace:excelConvert.ViewModels"
        mc:Ignorable="d"
        Title="配置转换器" Height="800" Width="800" MinHeight="700" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <!-- 引用样式文件 -->
        <ResourceDictionary Source="Styles/AppStyles.xaml"/>
    </Window.Resources>
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#f5f7fa" Offset="0"/>
                <GradientStop Color="#c3cfe2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Excel文件和Sheet树形结构 -->
        <Border Grid.Row="0" Style="{StaticResource ContentBorder}" Margin="15,5,15,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Excel文件" Style="{StaticResource TitleText}" Margin="15,10,0,0"/>
                <TreeView Grid.Row="1" ItemsSource="{Binding ExcelFileTree}" Margin="10" Height="200" SelectedItemChanged="TreeView_SelectedItemChanged" MouseDoubleClick="TreeView_MouseDoubleClick">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Sheets}">
                            <TextBlock Text="{Binding Name}" Cursor="Hand"/>
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" Cursor="Hand"/>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
        </Border>
        
        <!-- 状态显示区域 -->
        <Border Grid.Row="1" Style="{StaticResource StatusPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource StatusMessageText}" Grid.Row="0"/>
                <ProgressBar Grid.Row="1" IsIndeterminate="{Binding IsBusy}" Style="{StaticResource CustomProgressBar}" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>
        
        <!-- 数据预览区域 -->
        <Grid Grid.Row="2" Margin="15">
            <Border Style="{StaticResource ContentBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="数据预览" Style="{StaticResource TitleText}" Margin="15,20,0,0"/>
                    <TextBlock Text="Excel文件数据将在此处显示" Style="{StaticResource StatusText}" Grid.Row="0"/>
                    
                    <ScrollViewer Grid.Row="1" Style="{StaticResource CustomScrollViewer}" Margin="0,5,0,10">
                        <ItemsControl ItemsSource="{Binding PreviewData}" HorizontalAlignment="Stretch">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Style="{StaticResource PreviewText}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
        
        <Grid Grid.Row="3" Margin="15,5,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Content="全部导出"
                    Command="{Binding ExportAllCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Width="120" Height="36"
                    Margin="0,0,0,5"/>
            <TextBlock Grid.Column="2" Text="© 2026 Excel配置转换器" Style="{StaticResource StatusText}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
